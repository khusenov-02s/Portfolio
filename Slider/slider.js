let sliderImg=document.querySelectorAll(".slider__img"),slider=document.querySelector(".slider__container"),counter=0,counterMax=sliderImg.length-1;function swipeR(){let e=100/sliderImg.length;counter<counterMax?counter++:counter=0,slider.style=`\n    width: ${100*sliderImg.length}%;\n    transform: translateX(-${e*counter}%);\n  `}function swipeL(){let e=100/sliderImg.length;counter>0?counter--:counter=counterMax,slider.style=`\n    width: ${100*sliderImg.length}%;\n    transform: translateX(-${e*counter}%);\n  `}slider.style.width=`${100*sliderImg.length}%`;let btnLeft=document.querySelector(".btn-left"),btnRight=document.querySelector(".btn-right");btnRight.addEventListener("click",swipeR),btnLeft.addEventListener("click",swipeL),setInterval(swipeR,3e3);